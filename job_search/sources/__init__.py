"""
Source clients. Each module exposes a `fetch(criteria)` function returning
a list of normalised Listing dicts with keys:

    source, external_id, title, company, location, description, url,
    salary_min, salary_max, salary_is_day_rate, contract_type, posted_at
"""
